{% extends 'base.html.twig' %}

{% block title %}Catégorie d'image{% endblock %}

{% block body %}
    <h1>Categorie d'image</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Nom de la catégorie</th>
                <td>{{ categorie_image.libelleCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_image_index') }}">retour à la liste</a>

    <a href="{{ path('categorie_image_edit', {'id': categorie_image.id}) }}">éditer</a>

    {{ include('admin/categorie_image/_delete_form.html.twig') }}
{% endblock %}
