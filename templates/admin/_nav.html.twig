<nav class="navbar navbar-dark navbar-expand-lg bg-primary" id="nav">

    <a class="navbar-brand" href="{{ path('accueil') }}">Hugo coiffure Mixte</a><button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="true" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      {% if selected=='accueil' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('accueil_admin') }}">ACCUEIL
          <span class="sr-only"></span>
        </a>
      </li>
      {% if selected=='horaires' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('infos') }}">GESTION DES HORAIRES</a>
      </li>
      {% if selected=='blog' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('blog') }}">GESTION DU BLOG</a>
      </li>

      {% if selected=='contact' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('contact') }}">MESSAGERIE</a>
      </li>

      {% if selected=='prestations' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('prestations') }}">GESTION DES PRESTATIONS</a>
      </li>

      {% if selected=='photos' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('image_diaporama_index') }}">GESTION DES PHOTOS</a>
      </li>
      {% if selected=='categoriePhoto' %}
        <li class="nav-item active">
      {% else %}
        <li class="nav-item">
      {% endif %}
        <a class="nav-link" href="{{ path('categorie_image_index') }}">GESTION DES CATEGORIE DE PHOTOS</a>
      </li>
      
    </ul>
    
  </div>

</nav>